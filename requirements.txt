# ╔══════════════════════════════════════════════════════════════╗
# ║  Easy-Study RAG System – Python Dependencies               ║
# ║  Install:  pip install -r requirements.txt                  ║
# ║  Python:   ≥ 3.10                                          ║
# ╚══════════════════════════════════════════════════════════════╝

# ── Core Framework ──────────────────────────────────────────────
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
python-multipart>=0.0.9

# ── LangChain & Ecosystem ────────────────────────────────────────
# Pinned to compatible versions sharing the same langsmith constraint
langchain>=0.3.0,<0.3.15
langchain-community>=0.3.0,<0.3.15
langchain-core>=0.3.0,<0.3.15

# ── Local LLM (Ollama) ───────────────────────────────────────────
langchain-ollama>=0.2.0,<1.0

# ── OpenAI Fallback + OpenRouter (both use openai package) ───────
openai>=1.50.0,<2.0

# ── Google Gemini API (FREE) ─────────────────────────────────────
google-genai>=1.0.0
langchain-openai>=0.2.0,<1.0

# ── Embeddings ───────────────────────────────────────────────────
sentence-transformers>=3.0.0,<4.0
langchain-huggingface>=0.1.0,<1.0

# ── Vector Store ─────────────────────────────────────────────────
faiss-cpu>=1.8.0,<2.0

# ── Document Loaders ─────────────────────────────────────────────
pypdf>=4.0.0,<5.0
beautifulsoup4>=4.12.0,<5.0
requests>=2.31.0,<3.0
lxml>=5.0.0                          # robust HTML parsing for WebBaseLoader

# ── YouTube Transcript ───────────────────────────────────────────
# NOTE: v0.6.x static API was removed in v1.0+ — code uses new instance API
youtube-transcript-api>=1.0.0,<2.0

# ── Utilities ────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0
pydantic>=2.7.0,<3.0
pydantic-settings>=2.0.0,<3.0
httpx>=0.27.0,<1.0
aiofiles>=23.2.0,<25.0
tenacity>=8.2.0,<10.0

# ── Logging (structured) ────────────────────────────────────────
loguru>=0.7.0,<1.0

# ── Rate Limiting ────────────────────────────────────────────────
slowapi>=0.1.9,<1.0
